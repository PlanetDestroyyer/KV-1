# KV-1 Core Dependencies

# Note: HSOKV is required for memory system
# Install from: https://github.com/PlanetDestroyyer/hsokv
# Will be added as git submodule or installed separately

# Core Libraries
torch>=2.0.0                    # PyTorch for GPU-accelerated tensors
sentence-transformers>=2.2.0    # Semantic embeddings (384-D vectors)
sympy>=1.12                     # Symbolic mathematics (MathConnect)
numpy>=1.24.0                   # Array operations

# Web & API
requests>=2.31.0                # HTTP requests for web research
beautifulsoup4>=4.12.0          # HTML parsing for web scraping
ollama>=0.3.0                   # Local LLM (qwen3:4b thinking model)
google-generativeai>=0.3.0      # Optional: Gemini API support

# Utilities
python-dotenv>=1.0.0            # Environment variable management
